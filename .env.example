# Copy this file to .env and fill the values before running

# Database
DB_NAME=wordpress
DB_USER=wp
DB_PASSWORD=change_me_strong_password
DB_ROOT_PASSWORD=change_root_password

# Host ports
WP_PORT=8080
PHPMYADMIN_PORT=8081

# Optional
# WORDPRESS_TABLE_PREFIX=wp_
# WORDPRESS_DEBUG=0
